if height >= 140:
    print("A")

elif height >= 120:
    print("B")

else:
    print("어린이")

    # 컴퓨터는 조건이 만족하면 그 아래 함수는 더 이상 보지 않음 
    # if 조건 만족하면 끝 
    # if if if 쓰면 또 그 아래 조건 확인해서 이런 식으로 사용 x

    # 조건을 각각 독립적으로 검사 -> if를 여러개 
    # 여러 조건 중 하나만 선택 -> if - elif - else
